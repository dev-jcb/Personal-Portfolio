import React from "react";
import {Route, Routes } from "react-router-dom";

const MainRouter = () => {
    return (
        <div>
        <Routes>
        {/* add routes here */}
        </Routes>
        </div>
    );
};

export default MainRouter;